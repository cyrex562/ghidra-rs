//! Minimal forward-reference placeholders for not-yet-ported `ghidra.app.util.bin` types,
//! needed only so that already-ported dependents of this module can compile before the real
//! type lands. See `STUBS.tsv` for the promotion ledger.
//!
//! (Empty for now: the `ByteProvider` placeholder formerly here was promoted to the real port at
//! [`crate::app::util::bin::byte_provider`].)
