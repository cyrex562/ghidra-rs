//! UI components for the Symbolic Summarizer.

#[cfg(test)]
mod tests {
    #[test]
    fn module_exists() {
        // Package-info port: verifies the symz3 gui field module is wired into the crate.
    }
}
